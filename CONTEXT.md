# AX 평가 시스템

AX 평가 시스템에서 구현 범위와 상태를 일관되게 구분하기 위한 용어집이다.

## Language

**통합 완료 기능**:
작업 기준 시점의 `main` 브랜치에 소스와 테스트가 이미 병합된 기능. 미병합 브랜치의 코드는 완성도로 추정하지 않고 제외한다.
_Avoid_: 팀원이 작업한 기능, 완료된 것 같은 기능

**미작업 영역**:
`main`에서 스텁·미리보기·가짜 데이터·미연결 상태로 명시되었거나 미병합 브랜치에만 존재하는 기능. 이번 프로덕션 하드닝에서 새로 구현하지 않는다.
_Avoid_: 나중에 연결할 기능, 거의 완성된 기능

**개발 시연 데이터**:
로컬에서 화면을 확인할 때 쓰는 데이터는 `seed_data.py`로만 만든다 - 실제 모델에 실제 계산
서비스를 태워 넣으므로 화면은 언제나 DB를 통해 렌더링된다. (가짜 데이터로 화면만 그리던
`ENABLE_DEV_PREVIEWS` 프리뷰 라우트는 결과·공개 화면이 DB에 연결되면서 제거됐다.)
_Avoid_: 화면 전용 가짜 데이터, DB를 우회하는 미리보기 라우트

**지원 인증 경로**:
현재 `main`에 통합된 비밀번호 로그인, 공개 회원가입, Google·Kakao 소셜 로그인과 화이트리스트 승인 흐름의 총칭. 정제 요구사항의 초기 MVP 경계보다 넓지만 이번 통합에서 함께 유지한다.
_Avoid_: MVP 인증, Google 전용 로그인

**이메일 소유 확인**:
사용자가 해당 주소로 발송된 일회성 확인 링크를 완료했거나 소셜 공급자가 검증된 이메일임을 보증한 상태. 이메일 문자열을 입력하거나 화이트리스트에서 일치한 것만으로는 소유 확인으로 보지 않는다.
_Avoid_: 이메일 확인, 화이트리스트 일치

**애플리케이션 관리자**:
`role=ADMIN`, `is_staff=True`로 표시되고 승인·활성 상태인 사용자. 애플리케이션 관리 기능은 이 조건으로 허용하며, Django Admin은 여기에 모델별 permission을 추가로 확인한다. `is_superuser=True`는 staff·ADMIN·APPROVED를 항상 동반하지만 사고 대응을 위해 `is_active=False`로 정지할 수 있고, 정지 상태에서는 어떤 관리 접근도 허용하지 않는다.
_Avoid_: 관리자 플래그, staff와 admin을 혼용
