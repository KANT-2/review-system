# AX 평가 시스템

AX 평가 시스템에서 구현 범위와 상태를 일관되게 구분하기 위한 용어집이다.

## Language

**통합 완료 기능**:
작업 기준 시점의 `main` 브랜치에 소스와 테스트가 이미 병합된 기능. 미병합 브랜치의 코드는 완성도로 추정하지 않고 제외한다.
_Avoid_: 팀원이 작업한 기능, 완료된 것 같은 기능

**미작업 영역**:
`main`에서 스텁·미리보기·가짜 데이터·미연결 상태로 명시되었거나 미병합 브랜치에만 존재하는 기능. 이번 프로덕션 하드닝에서 새로 구현하지 않는다.
_Avoid_: 나중에 연결할 기능, 거의 완성된 기능

**개발 시연 데이터**:
로컬에서 화면을 확인할 때 쓰는 데이터는 `seed_data.py`로만 만든다 - 실제 모델에 실제 계산
서비스를 태워 넣으므로 화면은 언제나 DB를 통해 렌더링된다. (가짜 데이터로 화면만 그리던
`ENABLE_DEV_PREVIEWS` 프리뷰 라우트는 결과·공개 화면이 DB에 연결되면서 제거됐다.)
_Avoid_: 화면 전용 가짜 데이터, DB를 우회하는 미리보기 라우트

**지원 인증 경로**:
현재 `main`에 통합된 비밀번호 로그인, 공개 회원가입, Google·Kakao 소셜 로그인과 화이트리스트 승인 흐름의 총칭. 정제 요구사항의 초기 MVP 경계보다 넓지만 이번 통합에서 함께 유지한다.
_Avoid_: MVP 인증, Google 전용 로그인

**이메일 소유 확인**:
소셜 공급자가 검증된 이메일임을 보증한 상태. 발신 도메인의 PTR을 설정할 수 없어 확인 링크 발송은 걷어냈으므로, 비밀번호 가입 경로에는 소유 확인 단계가 없다. 이메일 문자열을 입력하거나 화이트리스트에서 일치한 것만으로는 소유 확인으로 보지 않는다.
_Avoid_: 이메일 확인, 화이트리스트 일치, 확인 링크

**애플리케이션 관리자**:
`role=ADMIN`, `is_staff=True`로 표시되고 승인·활성 상태인 사용자. 애플리케이션 관리 기능은 이 조건으로 허용하며, Django Admin은 여기에 모델별 permission을 추가로 확인한다. `is_superuser=True`는 staff·ADMIN·APPROVED를 항상 동반하지만 사고 대응을 위해 `is_active=False`로 정지할 수 있고, 정지 상태에서는 어떤 관리 접근도 허용하지 않는다.
_Avoid_: 관리자 플래그, staff와 admin을 혼용

**평가 확정**:
평가자가 유형(팀/개인)별로 `최종 제출`을 눌러 잠근 상태. 개별 제출은 확정이 아니며, 확정 전까지는 같은 대상에 몇 번이든 다시 저장할 수 있다(대상별 제출은 계속 한 건으로 유지된다).
_Avoid_: 제출 완료, 임시저장

## 정제 요구사항에서 의도적으로 벗어난 범위

`docs/REFINED-REQUIREMENTS.md`가 정한 MVP 경계보다 넓거나 다르게 구현한 부분이다. 다음 사람이 명세와 코드가 어긋난 것으로 오해하지 않도록 남긴다.

| 항목 | 명세 | 현재 코드 | 이유 |
|---|---|---|---|
| 인증 경로 | §2.3 공개 회원가입·자동 계정 생성·화이트리스트 승인·강제 온보딩·Google 외 소셜 로그인 제외 | 모두 구현 | 통합 시점에 함께 유지하기로 결정 |
| 이메일 소유 확인 | AUTH-006 사전 등록 계정에만 최초 연결 | 확인 링크 자체를 제거, 가입 시 비밀번호를 바로 설정 | 발신 도메인 PTR을 설정할 수 없어 메일 발송이 불가능 |
| 평가 수정 | TR-003·PR-003 저장 후 학생이 수정·삭제할 수 없다 | 유형별 최종 제출 전까지 다시 저장 가능 | 잘못 낸 학생에게 되돌릴 방법이 없었다 |
| 일괄 최종 제출 | §2.3 전체 평가 일괄 최종 제출 제외 | 유형별 최종 제출로 구현 | 위 수정 허용과 짝을 이루는 확정 시점이 필요 |
| 마이페이지 역량 분석 | §2.3 레이더 차트·역량 분석·통계 그래프·Excel 다운로드 제외 | 문항에 역량 태그를 달아 5대 역량 레이더차트·강점/보완 역량, 회차별 팀·개인 점수 막대그래프, 회차 결과 CSV 다운로드를 마이페이지에 구현 | 학생 본인 동기부여를 위해 의도적으로 넣기로 결정. 단, 주관식 피드백을 학생에게 공개하는 부분(같은 §2.3 항목)은 이번에도 그대로 제외했다 |

임시저장(`DRAFT` 상태)은 넣지 않았다. 최종 제출 전 수정이 같은 목적을 달성한다.

역량 태그가 없는 문항(과거에 만들어졌거나 튜터가 지정하지 않은 문항)은 역량 집계에서 조용히 빠진다 - 소급 적용하지 않기로 했다.
